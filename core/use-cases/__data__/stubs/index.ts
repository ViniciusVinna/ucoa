import accountsStub from "./accounts.stub.json";
import accountsTreeStub from "./accounts-tree.stub.json";

export const stubs = {
  accounts: accountsStub as AccountBase[],
  accountsTree: accountsTreeStub as AccountEntity[],
};